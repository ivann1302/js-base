# Зависимости (установим заново в контейнере)
node_modules

# Собранные файлы (соберем в контейнере)
dist
dist-ssr

# Git (не нужен в контейнере)
.git
.gitignore

# Редакторы (не нужны)
.vscode
.idea

# Логи (не нужны)
*.log
npm-debug.log*
yarn-debug.log*

# Системные файлы
.DS_Store

# Переменные окружения (если есть)
.env
.env.local

# Документация (не нужна в контейнере)
README.md
PLAN.md

# CI/CD (не нужен в контейнере)
.github